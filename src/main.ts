// Main entry point - initializes Alpine.js and imports global styles
import './style.css'
import Alpine from 'alpinejs'

// Make Alpine available globally for inline x-data attributes
window.Alpine = Alpine

// Start Alpine.js
Alpine.start()
